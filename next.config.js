/** @type {import('next').NextConfig} */
const nextConfig = {
  // All API keys (ETHERSCAN_API_KEY, GEMINI_API_KEY, COINGECKO_API_KEY)
  // are server-side only — used in app/api/ routes.
  // NEVER expose API keys to client side.
};

module.exports = nextConfig;
